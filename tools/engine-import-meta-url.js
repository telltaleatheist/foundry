// Injected by tools/build-engine.mjs: what `import.meta.url` means inside the
// CommonJS engine bundle — the bundle's own file URL. See that script.
export const __foundryEngineUrl = require('node:url').pathToFileURL(__filename).href;
